# Local-only: keep Android / Swagger on :8000; Nginx on :8080 (Windows often blocks :80).

services:
  api:
    ports:
      - "${API_PORT:-8000}:8000"

  nginx:
    ports:
      - "${HTTP_PORT:-8080}:80"
